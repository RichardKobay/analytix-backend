remember that should install it with cuda to use the gpu: https://pytorch.org/get-started/locally/

# Setup

```
pip install -r requirements.txt
```

then run

```
python setup.py
```

finally just run main
